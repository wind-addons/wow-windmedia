{
	"private": true,
	"scripts": {
		"update-vendor": "bun run update-vendor.ts",
		"refresh-vendor": "bun run update-vendor.ts --refresh",
		"lint": "oxlint .",
		"lint:fix": "oxlint --fix .",
		"format:check": "oxfmt --check .",
		"format": "oxfmt ."
	},
	"devDependencies": {
		"@types/bun": "^1.3.12",
		"@types/node": "^25.6.0",
		"oxfmt": "^0.46.0",
		"oxlint": "^1.61.0",
		"oxlint-tsgolint": "^0.21.1"
	}
}
